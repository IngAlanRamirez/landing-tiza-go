{
  "name": "landing-tiza-go",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "bun run build && gh-pages -d dist",
    "deploy:force": "git commit --allow-empty -m '🚀 Trigger manual deployment' && git push origin main"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.1",
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^5.11.0",
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
