---
// Componente Hero principal para Tiza Go con glasmorfismo y animaciones dinámicas
---

<header
  class="min-h-screen flex flex-col items-center justify-center relative overflow-hidden px-4 sm:px-6 lg:px-8"
>
  <!-- Hero glassmorphism container -->
  <div
    class="glass rounded-3xl p-6 sm:p-8 md:p-12 lg:p-16 max-w-6xl mx-auto text-center relative z-20 animate-fade-in w-full"
  >
    <!-- Logo con fondo contrastante y efectos mejorados -->
    <div class="relative mb-6 sm:mb-8 group">
      <!-- Fondo contrastante para el logo -->
      <div
        class="absolute inset-0 bg-white/95 backdrop-blur-sm rounded-3xl transform scale-110 opacity-90 group-hover:opacity-100 transition-all duration-300 animate-pulse-slow"
      >
      </div>
      <div
        class="absolute inset-0 bg-gradient-to-br from-white via-gray-50 to-white rounded-3xl transform scale-105 shadow-2xl animate-glow-strong"
      >
      </div>

      <!-- Logo principal -->
      <div class="relative z-10 p-4 sm:p-6 md:p-8">
        <img
          src={`${import.meta.env.BASE_URL}/TizaGO.png`}
          alt="Logo Tiza Go - Aplicación de entregas local de Tizayuca"
          class="w-32 h-32 sm:w-40 sm:h-40 md:w-48 md:h-48 lg:w-56 lg:h-56 xl:w-72 xl:h-72 2xl:w-80 2xl:h-80 mx-auto drop-shadow-2xl object-contain transform transition-transform duration-300 group-hover:scale-105 animate-float-gentle"
          loading="eager"
          decoding="async"
          fetchpriority="high"
        />
      </div>

      <!-- Efectos decorativos alrededor del logo -->
      <div
        class="absolute -inset-4 bg-gradient-to-r from-primary-500 via-accent-500 to-primary-500 rounded-3xl blur-xl opacity-30 animate-pulse group-hover:opacity-50 transition-opacity"
      >
      </div>
      <div
        class="absolute -inset-8 bg-gradient-to-r from-accent-400 via-primary-400 to-accent-400 rounded-full blur-2xl opacity-20 animate-spin-slow"
      >
      </div>
    </div>

    <!-- Título principal con animación de escritura -->
    <h1
      class="text-3xl sm:text-4xl md:text-6xl lg:text-7xl xl:text-8xl font-black mb-4 sm:mb-6 text-white leading-tight"
    >
      <span
        class="bg-gradient-to-r from-white via-primary-200 to-accent-300 bg-clip-text text-transparent animate-slide-in typewriter"
      >
        TIZA GO
      </span>
    </h1>

    <!-- Subtítulo con efecto de aparición secuencial -->
    <p
      class="text-lg sm:text-xl md:text-3xl lg:text-4xl font-bold mb-3 sm:mb-4 text-white/90 animate-fade-in-up px-2"
      style="animation-delay: 0.3s;"
    >
      La app de entregas local más
      <span
        class="bg-gradient-to-r from-primary-400 to-accent-400 bg-clip-text text-transparent animate-bounce-gentle block sm:inline"
      >
        RÁPIDA
      </span>
      de Tizayuca
    </p>

    <!-- Slogan principal con efecto de deslizamiento -->
    <p
      class="text-base sm:text-lg md:text-2xl mb-6 sm:mb-8 text-white/80 font-medium animate-fade-in-left max-w-4xl mx-auto px-2"
      style="animation-delay: 0.6s;"
    >
      🚀 Conectamos tu negocio con toda la ciudad
      <br class="hidden sm:block" />
      <span class="text-accent-300 font-bold animate-pulse-gentle"
        >¡Muy pronto en tu colonia!</span
      >
    </p>

    <!-- Botones de acción con efectos mejorados -->
    <div
      class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center items-center mb-6 sm:mb-8 animate-fade-in-up px-2"
      style="animation-delay: 0.9s;"
    >
      <a
        href="https://wa.me/521XXXXXXXXXX?text=Hola%2C%20quiero%20registrar%20mi%20negocio%20en%20Tiza%20Go"
        target="_blank"
        rel="noopener"
        class="group relative bg-gradient-to-r from-primary-600 to-primary-700 hover:from-primary-700 hover:to-primary-800 text-white font-bold py-3 sm:py-4 px-6 sm:px-8 rounded-full shadow-2xl transition-all duration-300 transform hover:scale-105 hover:shadow-primary-500/50 animate-glow pulse-on-hover w-full sm:w-auto"
      >
        <span class="relative z-10 text-base sm:text-lg md:text-xl">
          💬 <span class="hidden sm:inline">Registra tu Negocio</span><span
            class="sm:hidden">Registrar</span
          > GRATIS
        </span>
        <div
          class="absolute inset-0 bg-gradient-to-r from-primary-500 to-accent-500 rounded-full blur opacity-0 group-hover:opacity-70 transition-opacity"
        >
        </div>
      </a>

      <a
        href="#features"
        class="group glass-dark hover:glass text-white font-semibold py-3 sm:py-4 px-6 sm:px-8 rounded-full shadow-xl transition-all duration-300 transform hover:scale-105 text-base sm:text-lg md:text-xl hover:shadow-white/20 smooth-scroll-link w-full sm:w-auto"
      >
        ⚡ Conoce más
      </a>
    </div>

    <!-- Badge de disponibilidad con efecto pulsante -->
    <div
      class="inline-flex items-center gap-2 glass-dark rounded-full px-4 sm:px-6 py-2 sm:py-3 animate-fade-in pulse-subtle text-sm sm:text-base"
      style="animation-delay: 1.2s;"
    >
      <div
        class="w-2 h-2 sm:w-3 sm:h-3 bg-accent-500 rounded-full animate-ping"
      >
      </div>
      <span class="text-white font-medium">Próximamente disponible</span>
    </div>
  </div>

  <!-- Imagen de fondo flotante mejorada -->
  <div
    class="absolute bottom-0 left-1/2 transform -translate-x-1/2 w-full max-w-xl sm:max-w-2xl opacity-15 sm:opacity-20 pointer-events-none select-none animate-float-slow z-10"
  >
    <img
      src={`${import.meta.env.BASE_URL}/background.svg`}
      alt="Ilustración de motocicleta de repartidor para delivery Tiza Go"
      class="w-full h-auto transform hover:scale-105 transition-transform duration-1000"
      loading="lazy"
      decoding="async"
    />
  </div>

  <!-- Elementos decorativos animados - responsivos -->
  <div
    class="absolute top-16 sm:top-20 left-4 sm:left-10 w-3 h-3 sm:w-4 sm:h-4 bg-primary-500 rounded-full animate-ping animate-float"
  >
  </div>
  <div
    class="absolute top-32 sm:top-40 right-8 sm:right-20 w-4 h-4 sm:w-6 sm:h-6 bg-accent-500 rounded-full animate-ping animate-bounce"
    style="animation-delay: 1s;"
  >
  </div>
  <div
    class="absolute bottom-32 sm:bottom-40 left-8 sm:left-20 w-2 h-2 sm:w-3 sm:h-3 bg-primary-400 rounded-full animate-ping animate-pulse"
    style="animation-delay: 2s;"
  >
  </div>

  <!-- Partículas flotantes - solo en pantallas grandes -->
  <div
    class="hidden sm:block absolute top-1/4 left-1/4 w-2 h-2 bg-white/30 rounded-full animate-float-up"
    style="animation-delay: 0.5s;"
  >
  </div>
  <div
    class="hidden md:block absolute top-1/3 right-1/3 w-1 h-1 bg-accent-400/50 rounded-full animate-float-up"
    style="animation-delay: 1.5s;"
  >
  </div>
  <div
    class="hidden sm:block absolute bottom-1/3 left-1/6 w-3 h-3 bg-primary-400/40 rounded-full animate-float-up"
    style="animation-delay: 2.5s;"
  >
  </div>

  <!-- Scroll indicator mejorado -->
  <div
    class="absolute bottom-4 sm:bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce z-20 group cursor-pointer"
    onclick="document.querySelector('#slogan-section').scrollIntoView({behavior: 'smooth'})"
  >
    <div
      class="w-5 h-8 sm:w-6 sm:h-10 border-2 border-white/30 rounded-full flex justify-center group-hover:border-white/60 transition-colors"
    >
      <div
        class="w-1 h-2 sm:h-3 bg-white/60 rounded-full mt-1 sm:mt-2 animate-pulse group-hover:bg-white/80 transition-colors"
      >
      </div>
    </div>
    <p
      class="text-white/60 text-xs mt-1 sm:mt-2 group-hover:text-white/80 transition-colors"
    >
      Scroll
    </p>
  </div>
</header>

<style>
  /* Nuevas animaciones personalizadas */
  @keyframes float-gentle {
    0%,
    100% {
      transform: translateY(0px) rotate(0deg);
    }
    50% {
      transform: translateY(-8px) rotate(1deg);
    }
  }

  @keyframes float-slow {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-15px);
    }
  }

  @keyframes float-up {
    0% {
      transform: translateY(100vh) scale(0);
      opacity: 0;
    }
    10% {
      opacity: 1;
    }
    90% {
      opacity: 1;
    }
    100% {
      transform: translateY(-100vh) scale(1);
      opacity: 0;
    }
  }

  @keyframes glow-strong {
    0%,
    100% {
      box-shadow:
        0 0 30px rgba(220, 38, 38, 0.3),
        0 0 60px rgba(245, 158, 11, 0.2);
    }
    50% {
      box-shadow:
        0 0 50px rgba(220, 38, 38, 0.5),
        0 0 100px rgba(245, 158, 11, 0.3);
    }
  }

  @keyframes pulse-slow {
    0%,
    100% {
      opacity: 0.8;
    }
    50% {
      opacity: 1;
    }
  }

  @keyframes spin-slow {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes bounce-gentle {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-5px);
    }
  }

  @keyframes fade-in-up {
    0% {
      opacity: 0;
      transform: translateY(30px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fade-in-left {
    0% {
      opacity: 0;
      transform: translateX(-30px);
    }
    100% {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes typewriter {
    0% {
      width: 0;
      opacity: 0;
    }
    50% {
      opacity: 1;
    }
    100% {
      width: 100%;
      opacity: 1;
    }
  }

  /* Aplicar animaciones */
  .animate-float-gentle {
    animation: float-gentle 4s ease-in-out infinite;
  }

  .animate-float-slow {
    animation: float-slow 6s ease-in-out infinite;
  }

  .animate-float-up {
    animation: float-up 15s linear infinite;
  }

  .animate-glow-strong {
    animation: glow-strong 3s ease-in-out infinite;
  }

  .animate-pulse-slow {
    animation: pulse-slow 4s ease-in-out infinite;
  }

  .animate-spin-slow {
    animation: spin-slow 20s linear infinite;
  }

  .animate-bounce-gentle {
    animation: bounce-gentle 2s ease-in-out infinite;
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
    opacity: 0;
  }

  .animate-fade-in-left {
    animation: fade-in-left 0.8s ease-out forwards;
    opacity: 0;
  }

  .pulse-on-hover:hover {
    animation: glow 0.5s ease-in-out;
  }

  .pulse-subtle {
    animation: pulse-slow 3s ease-in-out infinite;
  }

  .pulse-gentle {
    animation: bounce-gentle 2s ease-in-out infinite;
  }

  /* Scroll suave */
  .smooth-scroll-link {
    scroll-behavior: smooth;
  }
</style>

<script>
  // Animaciones de scroll reveal
  const observerOptions = {
    threshold: 0.1,
    rootMargin: "0px 0px -50px 0px",
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("animate-fade-in");
      }
    });
  }, observerOptions);

  // Observar elementos para animaciones de scroll
  document.addEventListener("DOMContentLoaded", () => {
    const elementsToAnimate = document.querySelectorAll(".animate-on-scroll");
    elementsToAnimate.forEach((el) => observer.observe(el));

    // Efecto de parallax suave
    window.addEventListener("scroll", () => {
      const scrolled = window.pageYOffset;
      const parallaxElements = document.querySelectorAll(".parallax");

      parallaxElements.forEach((element) => {
        const htmlElement = element as HTMLElement;
        const speed = htmlElement.dataset.speed || "0.5";
        htmlElement.style.transform = `translateY(${scrolled * parseFloat(speed)}px)`;
      });
    });
  });
</script>
