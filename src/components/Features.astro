---
// Componente de características/beneficios de Tiza Go con animaciones dinámicas
const features = [
  {
    icon: "🚀",
    title: "Entregas locales en minutos",
    description: "La velocidad que necesitas para tu negocio",
    color: "from-primary-500 to-primary-700",
  },
  {
    icon: "🏪",
    title: "Negocios de Tizayuca, 100% local",
    description: "Apoyamos la economía local de nuestra ciudad",
    color: "from-accent-500 to-accent-700",
  },
  {
    icon: "💬",
    title: "Contacto directo por WhatsApp",
    description: "Comunicación directa y personal",
    color: "from-green-500 to-green-700",
  },
  {
    icon: "⚡",
    title: "Más rápido que cualquier app externa",
    description: "Optimizado para Tizayuca específicamente",
    color: "from-primary-600 to-secondary-600",
  },
  {
    icon: "🍲",
    title: "Ideal para tianguis, fondas y cocinas",
    description: "Perfecto para negocios locales tradicionales",
    color: "from-accent-600 to-primary-600",
  },
  {
    icon: "🎯",
    title: "Cobertura total en Tizayuca",
    description: "Todas las colonias, todos los rincones",
    color: "from-secondary-600 to-primary-600",
  },
];
---

<section id="features" class="py-20 relative overflow-hidden animate-on-scroll">
  <div class="max-w-7xl mx-auto px-4">
    <!-- Título de la sección con entrada dramática -->
    <div class="text-center mb-16 scroll-reveal" data-delay="100">
      <h2 class="text-4xl md:text-5xl lg:text-6xl font-black text-white mb-6 transform transition-all duration-1000">
        <span class="bg-gradient-to-r from-primary-400 to-accent-400 bg-clip-text text-transparent hover:from-accent-400 hover:to-primary-400 transition-all duration-500">
          ¿Por qué elegir
        </span>
        <br>
        <span class="text-white animate-pulse-gentle">TIZA GO?</span>
      </h2>
      <p class="text-xl md:text-2xl text-white/80 max-w-3xl mx-auto transform transition-all duration-1000" style="animation-delay: 0.3s;">
        Descubre las ventajas de trabajar con la plataforma de entregas más innovadora de Tizayuca
      </p>
    </div>
    
    <!-- Grid de características con animaciones escalonadas -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {features.map((feature, index) => (
        <div 
          class="group glass hover:glass-dark rounded-3xl p-8 text-center relative overflow-hidden transform transition-all duration-700 hover:scale-105 hover:shadow-2xl cursor-pointer scroll-reveal feature-card"
          data-delay={index * 100}
          style={`animation-delay: ${index * 0.1}s;`}
        >
          <!-- Efecto de hover gradient -->
          <div class={`absolute inset-0 bg-gradient-to-br ${feature.color} opacity-0 group-hover:opacity-20 transition-all duration-500 rounded-3xl`}></div>
          
          <!-- Línea de progreso en hover -->
          <div class="absolute top-0 left-0 w-0 h-1 bg-gradient-to-r from-primary-500 to-accent-500 group-hover:w-full transition-all duration-700 rounded-t-3xl"></div>
          
          <!-- Contenido -->
          <div class="relative z-10">
            <!-- Icono con efectos avanzados -->
            <div class="relative mb-6 icon-container">
              <div class="text-6xl md:text-7xl mb-4 transform transition-all duration-500 group-hover:scale-110 group-hover:rotate-12 filter group-hover:drop-shadow-2xl">
                {feature.icon}
              </div>
              <!-- Aura alrededor del icono -->
              <div class={`absolute inset-0 bg-gradient-to-r ${feature.color} rounded-full blur-xl opacity-0 group-hover:opacity-30 transition-all duration-500 animate-pulse-slow`}></div>
              <!-- Anillo de energía -->
              <div class="absolute inset-0 border-2 border-transparent group-hover:border-white/20 rounded-full transition-all duration-500 animate-spin-slow"></div>
            </div>
            
            <!-- Título con efecto de escritura -->
            <h3 class="text-xl md:text-2xl font-bold text-white mb-4 group-hover:text-white transition-all duration-300 transform group-hover:scale-105">
              {feature.title}
            </h3>
            
            <!-- Descripción con reveal -->
            <p class="text-white/70 group-hover:text-white/90 transition-all duration-300 leading-relaxed transform group-hover:translate-y-[-2px]">
              {feature.description}
            </p>
            
            <!-- Indicador de interactividad -->
            <div class="mt-4 flex justify-center opacity-0 group-hover:opacity-100 transition-all duration-300">
              <div class="w-12 h-1 bg-gradient-to-r from-primary-500 to-accent-500 rounded-full"></div>
            </div>
          </div>
          
          <!-- Elementos decorativos animados -->
          <div class="absolute top-4 right-4 w-4 h-4 bg-white/20 rounded-full opacity-0 group-hover:opacity-100 transition-all duration-300 animate-ping"></div>
          <div class="absolute bottom-4 left-4 w-3 h-3 bg-white/20 rounded-full opacity-0 group-hover:opacity-100 transition-all duration-300 animate-ping" style="animation-delay: 0.5s;"></div>
          
          <!-- Partículas flotantes -->
          <div class="absolute top-1/3 left-1/6 w-1 h-1 bg-white/30 rounded-full opacity-0 group-hover:opacity-100 animate-float-particle transition-opacity duration-300"></div>
          <div class="absolute bottom-1/3 right-1/6 w-2 h-2 bg-accent-400/30 rounded-full opacity-0 group-hover:opacity-100 animate-float-particle transition-opacity duration-300" style="animation-delay: 0.8s;"></div>
        </div>
      ))}
    </div>
    
    <!-- Call to action con efectos espectaculares -->
    <div class="text-center mt-16 scroll-reveal" data-delay="600">
      <div class="glass rounded-3xl p-8 md:p-12 inline-block relative overflow-hidden group hover:glass-dark transition-all duration-700 transform hover:scale-105">
        <!-- Efecto de onda en el fondo -->
        <div class="absolute inset-0 bg-gradient-to-r from-primary-500/10 via-accent-500/20 to-primary-500/10 rounded-3xl opacity-0 group-hover:opacity-100 transition-all duration-700 animate-wave"></div>
        
        <h3 class="text-2xl md:text-3xl font-bold text-white mb-4 transform group-hover:scale-105 transition-all duration-300">
          🎉 ¡Únete a la revolución de entregas locales!
        </h3>
        <p class="text-white/80 text-lg mb-6 max-w-2xl mx-auto transform group-hover:translate-y-[-2px] transition-all duration-300">
          Forma parte de la comunidad de negocios más exitosa de Tizayuca
        </p>
        
        <!-- Contador dinámico -->
        <div class="flex justify-center gap-8 mb-6">
          <div class="text-center">
            <div class="text-2xl md:text-3xl font-black text-primary-400 counter" data-target="100">0</div>
            <div class="text-white/60 text-sm">Negocios Interesados</div>
          </div>
          <div class="text-center">
            <div class="text-2xl md:text-3xl font-black text-accent-400 counter" data-target="50">0</div>
            <div class="text-white/60 text-sm">Repartidores Listos</div>
          </div>
        </div>
        
        <a
          href="https://wa.me/521XXXXXXXXXX?text=Quiero%20saber%20más%20sobre%20Tiza%20Go"
          target="_blank"
          rel="noopener"
          class="group relative bg-gradient-to-r from-primary-600 to-accent-600 hover:from-primary-700 hover:to-accent-700 text-white font-bold py-4 px-8 rounded-full shadow-2xl transition-all duration-300 transform hover:scale-110 animate-glow inline-block pulse-on-click"
        >
          <span class="relative z-10">
            💬 Quiero saber más
          </span>
          <div class="absolute inset-0 bg-gradient-to-r from-primary-500 to-accent-500 rounded-full blur opacity-0 group-hover:opacity-70 transition-opacity"></div>
          <!-- Ondas de clic -->
          <div class="absolute inset-0 rounded-full border-2 border-white/50 opacity-0 scale-0 click-wave"></div>
        </a>
      </div>
    </div>
  </div>
  
  <!-- Elementos decorativos de fondo mejorados -->
  <div class="absolute top-20 left-10 w-32 h-32 bg-primary-500/10 rounded-full blur-xl animate-float parallax" data-speed="0.3"></div>
  <div class="absolute bottom-20 right-10 w-40 h-40 bg-accent-500/10 rounded-full blur-xl animate-float parallax" data-speed="0.5" style="animation-delay: 1s;"></div>
  <div class="absolute top-1/2 left-1/4 w-24 h-24 bg-accent-400/5 rounded-full blur-xl animate-pulse-slow parallax" data-speed="0.2"></div>
</section>

<style>
  /* Animaciones avanzadas para Features */
  @keyframes wave {
    0%, 100% { transform: translateX(-100%) skewX(-15deg); }
    50% { transform: translateX(100%) skewX(-15deg); }
  }
  
  @keyframes float-particle {
    0%, 100% { transform: translateY(0px) translateX(0px) rotate(0deg); }
    25% { transform: translateY(-10px) translateX(5px) rotate(90deg); }
    50% { transform: translateY(-5px) translateX(-5px) rotate(180deg); }
    75% { transform: translateY(-15px) translateX(3px) rotate(270deg); }
  }
  
  @keyframes pulse-gentle {
    0%, 100% { opacity: 0.6; }
    50% { opacity: 1; }
  }
  
  @keyframes spin-slow {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
  
  @keyframes pulse-slow {
    0%, 100% { opacity: 0.8; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.05); }
  }
  
  @keyframes click-ripple {
    0% { opacity: 1; transform: scale(0); }
    100% { opacity: 0; transform: scale(2); }
  }
  
  /* Aplicar animaciones */
  .animate-wave {
    animation: wave 4s ease-in-out infinite;
  }
  
  .animate-float-particle {
    animation: float-particle 8s ease-in-out infinite;
  }
  
  .animate-pulse-gentle {
    animation: pulse-gentle 3s ease-in-out infinite;
  }
  
  .animate-spin-slow {
    animation: spin-slow 20s linear infinite;
  }
  
  .animate-pulse-slow {
    animation: pulse-slow 4s ease-in-out infinite;
  }
  
  /* Efectos de hover mejorados */
  .feature-card {
    backdrop-filter: blur(10px);
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }
  
  .feature-card:hover {
    backdrop-filter: blur(15px);
    background: rgba(15, 23, 42, 0.8);
    border: 1px solid rgba(255, 255, 255, 0.3);
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25), 0 0 30px rgba(220, 38, 38, 0.3);
  }
  
  /* Efecto de clic en botones */
  .pulse-on-click:active .click-wave {
    animation: click-ripple 0.6s ease-out;
    opacity: 1;
    transform: scale(0);
  }
  
  /* Scroll reveal */
  .scroll-reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s ease-out;
  }
  
  .scroll-reveal.revealed {
    opacity: 1;
    transform: translateY(0);
  }
</style>

<script>
  // Scroll reveal animation
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -100px 0px'
  };

      const revealObserver = new IntersectionObserver((entries) => {
      entries.forEach((entry, index) => {
        if (entry.isIntersecting) {
          const htmlElement = entry.target as HTMLElement;
          const delay = htmlElement.dataset.delay || '0';
          setTimeout(() => {
            entry.target.classList.add('revealed');
          }, parseInt(delay));
        }
      });
    }, observerOptions);

  // Counter animation
  function animateCounter(element: HTMLElement, target: number, duration = 2000) {
    const start = 0;
    const increment = target / (duration / 16);
    let current = start;
    
    const timer = setInterval(() => {
      current += increment;
      element.textContent = Math.floor(current).toString();
      
      if (current >= target) {
        element.textContent = target.toString();
        clearInterval(timer);
      }
    }, 16);
  }

  // Parallax effect
  function handleParallax() {
    const scrolled = window.pageYOffset;
    const parallaxElements = document.querySelectorAll('.parallax');
    
    parallaxElements.forEach((element) => {
      const htmlElement = element as HTMLElement;
      const speed = parseFloat(htmlElement.dataset.speed || '0.5');
      htmlElement.style.transform = `translateY(${scrolled * speed}px)`;
    });
  }

  // Initialize animations
  document.addEventListener('DOMContentLoaded', () => {
    // Scroll reveal
    const elementsToReveal = document.querySelectorAll('.scroll-reveal');
    elementsToReveal.forEach(el => revealObserver.observe(el));
    
    // Counters
    const counters = document.querySelectorAll('.counter');
    const counterObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const htmlElement = entry.target as HTMLElement;
          const target = parseInt(htmlElement.dataset.target || '0');
          animateCounter(htmlElement, target);
          counterObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.5 });
    
    counters.forEach(counter => counterObserver.observe(counter));
    
    // Parallax
    window.addEventListener('scroll', handleParallax);
    
    // Hover effects para feature cards
    const featureCards = document.querySelectorAll('.feature-card');
    featureCards.forEach(card => {
      const htmlCard = card as HTMLElement;
      card.addEventListener('mouseenter', () => {
        htmlCard.style.transform = 'scale(1.05) rotateY(5deg)';
      });
      
      card.addEventListener('mouseleave', () => {
        htmlCard.style.transform = 'scale(1) rotateY(0deg)';
      });
    });
  });
</script>
